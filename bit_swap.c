#include <stdio.h>

void bit_swap(int n, int x) {

    // 1234 6666

    // 1100 0011

    int res = 0;

    int mask = (1 << x) -1;
    
    n >> (31 - x) & mask

    
}

int main(int argc, char *argv[], char *envp[]) {
    
}